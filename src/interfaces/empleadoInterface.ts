// En una interface se definen los datos de cada tabla
export interface IEmpleado {
    idEmpleado?: string;
    nombre: string;
    apaterno: string;
    amaterno: string;
    direccion: string;
    telefono: string;
    ciudad: string;
    estado: string;
    usuario: string;
    password: string;
    rol: string;
}